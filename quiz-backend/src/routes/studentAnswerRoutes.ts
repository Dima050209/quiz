import { Router } from "express";
import { requireAuth } from "../middleware/requireAuth";

const router = Router();

// router.get('/:answerId', requireAuth)
// router.post('/', requireAuth)

export default router;